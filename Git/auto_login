git 사용 시, token을 이용하여 자동 로그인하는 방법

1. window
  - token 생성 후
  - C://사용자/사용자이름/.git-credentials 파일에 접근
  - https://sehooh5:<여기에 토큰값 입력>@github.com
  - $ git config --global credential.helper store <- store 로 저장하여 반영구적 사용
  
  
2. linux 에서 이미 clone된 repository 사용
  - toekn 생성 후
  - $ git remote set-url origin https://<token값>@github.com/<git ID>/<repository 이름>.git 설정해주기
